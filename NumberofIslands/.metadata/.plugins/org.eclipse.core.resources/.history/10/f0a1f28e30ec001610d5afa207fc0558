package Snippet;

public class CountIslands {
	
	private void DFS(int row, int col, int mat[][], String direction){
		if(row<0 || row>=mat.length){ return; }
		if(col<0 || col>=mat[0].length){ return; }
		
	}
}
